import { ComingSoon } from "@/components/dashboard/coming-soon";

export default function MonthPage() {
  return (
    <ComingSoon
      emoji="🗓️"
      title="Your whole month, at a glance"
      description="Every day as a little rounded card, filled in with the icons of the habits you kept. This is next on the build list — the auth and data layer underneath it is already in place."
    />
  );
}
